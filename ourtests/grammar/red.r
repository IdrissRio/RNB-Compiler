test : int;

proc procTest (x : const real , x : res string, x: char , x:valres °bool , x :ref °°int ) is 
    if 2 < 10 then
        x : int := 2;
        x := §x;
        return;
        break;
        continue;
        ++x;
        --x;
        x := +7+(-8)+9- p(+4);
        x--;
        x++;
        x := -1^2 *(-(++x++ - ++x++ -x++ - ++x--)) ;
    elseif 2<10 then
        x : int := 2;
        loop 
            x : int := 2;
            exiton x<=+(-(-(3)));
        end loop
        x : int := "String di Prova";
        x : int := 'c';
    else
        x : int := 3;
    end if
end procTest


func FuncTest () -> (str : string) is
    x : int :=0;
    loop 
        exiton x<=+(-(-(3)));
        exit;
        exiton "prova"=2;
        exiton "prova";
    end loop
end FuncTest

boolTest : bool := true and false or 2=2 and x+1<=3^2;
const constTest : int := §x;

arr : Array <Array <int>>:= [[1,2,3],[1,2,3]];
x : Array <int> := arr[0];
y : °int := §x[0];
z : °Array <int> := x;





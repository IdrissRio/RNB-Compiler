y : int := 8;

proc main() is
  const x : int := 6;
  if (x < y) then
     thenvar : char;
     proc f () is
          fthenvar : int;
     end f
  elseif (3 < 6) then
    elseifvar : char;
    return;
    f();
  else
     f();
     const x : int := 5;
     proc f() is
          thenvar : int := x;
     end f
  end if

  func f() -> (result : int) is
       return;
       outerf : int;
       result  := outerf;
  end f

end main
